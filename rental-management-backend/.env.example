# Backend Environment Variables Example

# Server configuration
PORT=4000
NODE_ENV=development

# Database configuration
# Use DB_DIALECT to switch between 'postgres' and 'mysql'; default is postgres.
# Set DB_SSL=true when connecting to a cloud provider (Neon, Heroku, etc.).
# If you are using a managed service that already supplies the database, disable
# creation step by setting SKIP_DB_SETUP=true.
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=rental_management
DB_SSL=false
SKIP_DB_SETUP=false

# Security
JWT_SECRET=your_jwt_secret_here

# CORS configuration (comma separated origins)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Email configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
FROM_NAME=Rental Management
FROM_EMAIL=your_email@gmail.com
